# Applications

Generated by the `job-apply-prep` skill. Each subfolder is one prepared
application package (CV, answers, notes); `tracker.csv` is the running log
across all of them. This folder is prep-only — nothing here is submitted
automatically, Vanshita applies manually on LinkedIn/Wellfound/Indeed.
